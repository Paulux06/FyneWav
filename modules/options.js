function spawnOptions(container=document.createElement('div')) {
    var title = document.createElement('h1');
    title.innerHTML = "Options";
    title.id = "app-options-title";
    container.appendChild(title);
}